node_modules
dist
out
.DS_Store
.eslintcache
*.log*
.env
.env.local
.idea/

# Android signing — never commit keystores or signing credentials
*.jks
*.keystore
android/keystore.properties

# iOS signing — never commit provisioning profiles or exported archives
*.p12
*.cer
*.mobileprovision
*.ipa
ios/App/ExportOptions.plist